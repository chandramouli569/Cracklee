const problemsData = [
    {
        "id": "2839",
        "date": "Mar 29, 2026",
        "title": "2839. Check if Strings Can be Made Equal With Operations",
        "difficulty": "Easy",
        "tags": "String, Simulation",
        "description": "<p>You are given two strings <code>s1</code> and <code>s2</code>, both of length 4, consisting of lowercase English letters.</p>\n<br>\n<p>You can apply the following operation on any of the two strings any number of times: Choose any two indices <code>i</code> and <code>j</code> such that <code>j - i = 2</code>, then swap the two characters at those indices in the string. Return <code>true</code> if you can make the strings equal, and <code>false</code> otherwise.</p>",
        "approach": "Since the strings are only of length 4, and we can only swap characters at distance 2, we can simply generate all valid permutations for both strings independently by swapping indices (0,2) and (1,3). If any generated permutation of s1 matches any generated permutation of s2, they can be made equal.",
        "cpp": "class Solution {\npublic:\n    bool canBeEqual(string s1, string s2) {\n        for (const string& a : swappedStrings(s1))\n            for (const string& b : swappedStrings(s2))\n                if (a == b)\n                    return true;\n        return false;\n    }\n\nprivate:\n    vector<string> swappedStrings(const string& s) {\n        vector<char> chars(s.begin(), s.end());\n        return {s, \n                string({chars[2], chars[1], chars[0], chars[3]}),\n                string({chars[0], chars[3], chars[2], chars[1]}),\n                string({chars[2], chars[3], chars[0], chars[1]})};\n    }\n};",
        "java": "class Solution {\n    public boolean canBeEqual(String s1, String s2) {\n        for (String a : swappedStrings(s1)) {\n            for (String b : swappedStrings(s2)) {\n                if (a.equals(b)) return true;\n            }\n        }\n        return false;\n    }\n\n    private List<String> swappedStrings(String s) {\n        char[] c = s.toCharArray();\n        return Arrays.asList(s, \n            new String(new char[]{c[2], c[1], c[0], c[3]}),\n            new String(new char[]{c[0], c[3], c[2], c[1]}),\n            new String(new char[]{c[2], c[3], c[0], c[1]}));\n    }\n}",
        "python": "class Solution:\n    def canBeEqual(self, s1: str, s2: str) -> bool:\n        def get_swaps(s):\n            yield s\n            yield s[2] + s[1] + s[0] + s[3]\n            yield s[0] + s[3] + s[2] + s[1]\n            yield s[2] + s[3] + s[0] + s[1]\n\n        return any(a == b for a in get_swaps(s1) for b in get_swaps(s2))",
        "timeComplexity": "O(1) - Generating the swapped strings and comparing them takes O(1) time since the string length is fixed at 4.",
        "spaceComplexity": "O(1) - We only store exactly 4 permutations of size 4."
    },
    {
        "id": "2840",
        "title": "Check if Strings Can be Made Equal With Operations II",
        "difficulty": "Medium",
        "tags": "Hash Table, String, Sorting",
        "description": "You are given two strings s1 and s2, both of length n, consisting of lowercase English letters.\n\nYou can apply the following operation on any of the two strings any number of times:\n\nChoose any two indices i and j such that i < j and the difference j - i is even, then swap the two characters at those indices in the string.\nReturn true if you can make the strings s1 and s2 equal, and false otherwise.\n\n \n\nExample 1:\n\nInput: s1 = \"abcdba\", s2 = \"cabdab\"\nOutput: true\nExplanation: We can apply the following operations on s1:\n- Choose the indices i = 0, j = 2. The resulting string is s1 = \"cbadba\".\n- Choose the indices i = 2, j = 4. The resulting string is s1 = \"cbbdaa\".\n- Choose the indices i = 1, j = 5. The resulting string is s1 = \"cabdab\" = s2.\nExample 2:\n\nInput: s1 = \"abe\", s2 = \"bea\"\nOutput: false\nExplanation: It is not possible to make the two strings equal.\n \n\nConstraints:\n\nn == s1.length == s2.length\n1 <= n <= 105\ns1 and s2 consist only of lowercase English letters.",
        "approach": "Approach: Hash Table\nIntuition\nThis problem is an extension of 「2839. Check if Strings Can Be Made Equal With Operations I」. We can generalize the case analysis from that problem to handle this more general scenario.\n\nFrom the given exchange rules, we can observe that characters can only be swapped with other characters that have the same parity of indices. In other words, characters at even indices can only be swapped among themselves, and similarly for characters at odd indices.\n\nThis implies that the string can be divided into two independent groups based on index parity, where characters within each group can be freely rearranged. Therefore, to determine whether s1​ can be transformed into s2,we only need to check whether the character multisets match for both the even-indexed positions and the odd-indexed positions. That is, both groups must have identical character frequencies in s1 and s2.\n\nThere are multiple ways to count character frequencies and compare them. A straightforward approach is to traverse s1\n​and s2\n​\nsimultaneously in a single loop. For each index, we increment the frequency of the character from s1 and decrement the frequency of the character from s2, keeping track separately for even and odd indices.\n\nAt the end, if all frequency counts are zero, it means the character distributions match in both groups; otherwise, they do not.",
        "cpp": "class Solution {\n public:\n  bool checkStrings(string s1, string s2) {\n    vector<vector<int>> count(2, vector<int>(26));\n\n    for (int i = 0; i < s1.length(); ++i) {\n      ++count[i % 2][s1[i] - 'a'];\n      --count[i % 2][s2[i] - 'a'];\n    }\n\n    for (int i = 0; i < 26; ++i)\n      if (count[0][i] > 0 || count[1][i] > 0)\n        return false;\n\n    return true;\n  }\n};",
        "java": "class Solution {\n  public boolean checkStrings(String s1, String s2) {\n    int[][] count = new int[2][26];\n\n    for (int i = 0; i < s1.length(); ++i) {\n      ++count[i % 2][s1.charAt(i) - 'a'];\n      --count[i % 2][s2.charAt(i) - 'a'];\n    }\n\n    for (int i = 0; i < 26; ++i)\n      if (count[0][i] > 0 || count[1][i] > 0)\n        return false;\n\n    return true;\n  }\n}",
        "python": "class Solution:\n  def checkStrings(self, s1: str, s2: str) -> bool:\n    count = [collections.Counter() for _ in range(2)]\n\n    for i, (a, b) in enumerate(zip(s1, s2)):\n      count[i % 2][a] += 1\n      count[i % 2][b] -= 1\n\n    return (all(freq == 0 for freq in count[0].values()) and\n            all(freq == 0 for freq in count[1].values()))",
        "timeComplexity": "Time complexity: O(n)",
        "spaceComplexity": "Space complexity: O(k)",
        "date": "Mar 30, 2026"
    },
    {
        "id": "3474",
        "title": "Lexicographically Smallest Generated String",
        "difficulty": "Hard",
        "tags": "string, greedy, principal",
        "description": "You are given two strings, str1 and str2, of lengths n and m, respectively.\n\nA string word of length n + m - 1 is defined to be generated by str1 and str2 if it satisfies the following conditions for each index 0 <= i <= n - 1:\n\nIf str1[i] == 'T', the substring of word with size m starting at index i is equal to str2, i.e., word[i..(i + m - 1)] == str2.\nIf str1[i] == 'F', the substring of word with size m starting at index i is not equal to str2, i.e., word[i..(i + m - 1)] != str2.\nReturn the lexicographically smallest possible string that can be generated by str1 and str2. If no string can be generated, return an empty string \"\".\n\n \n\nExample 1:\n\nInput: str1 = \"TFTF\", str2 = \"ab\"\n\nOutput: \"ababa\"\n\nExplanation:\n\nThe table below represents the string \"ababa\"\nIndex\tT/F\tSubstring of length m\n0\t'T'\t\"ab\"\n1\t'F'\t\"ba\"\n2\t'T'\t\"ab\"\n3\t'F'\t\"ba\"\nThe strings \"ababa\" and \"ababb\" can be generated by str1 and str2.\n\nReturn \"ababa\" since it is the lexicographically smaller string.\n\nExample 2:\n\nInput: str1 = \"TFTF\", str2 = \"abc\"\n\nOutput: \"\"\n\nExplanation:\n\nNo string that satisfies the conditions can be generated.\n\nExample 3:\n\nInput: str1 = \"F\", str2 = \"d\"\n\nOutput: \"a\"\n\n \n\nConstraints:\n\n1 <= n == str1.length <= 104\n1 <= m == str2.length <= 500\nstr1 consists only of 'T' or 'F'.\nstr2 consists only of lowercase English characters.",
        "approach": "Approach: Greedy\nIntuition\nWe first process the 'T' constraints, followed by the 'F' constraints.\n\nWe traverse all 'T' positions in str 1 and fill str 2 into the target string word. If a conflict occurs during this process, meaning a different character has already been assigned to the same position, then no valid solution exists, and we return an empty string. We also maintain a fixed array to record which positions are fixed by 'T' constraints.\n\nNext, for all positions that are not fixed, we assign the character 'a ' by default to ensure the lexicographically smallest result.\n\nWe then process the 'F' constraints. For each position i where str 1 [i]= ′ F ′ , we check whether the substring word[i…i+m−1] is exactly equal to str 2 . If it is not equal, no action is needed. However, if it is equal, we must modify at least one non-fixed character within this window to break the match.\n\nTo maintain the smallest lexicographical order, we choose to modify the rightmost non-fixed character in the window, changing it to 'b'.\n\nA natural concern is whether modifying the rightmost non-fixed character could invalidate a previously satisfied 'F' constraint. We now show that this cannot happen.\n\nSuppose str 1 contains two 'F' positions at indices i 1 and i 2. When processing i2, we modify a character in its window, which could potentially make the substring at i1 equal to str2, violating the 'F' constraint.\n\nConsider str2=A+ ′a ′ +B+ ′b ′+C.\nAt index i1, the substring is A+ ′a ′+B+ ′a ′+C, and at index i2\n, it is A+ ′a ′+B+ ′b ′+C. We modify the 'a' in the i2\n​window to 'b', which may affect the substring at i1.\n\nFrom this structure, we can derive the following:\n\n3474\n\nAs shown in the figure, we can obtain the following information:\n\nSince the rightmost non-fixed character in the i \n2\n​\n  window is the first 'a', there are no non-fixed characters to its right within the window. Also, the prefix B+ \n′\n b \n′\n +C must match a prefix of str \n2\n​\n  due to 'T' constraints.\nThe string A must be a suffix of A+ \n′\n a \n′\n +B.\nWe now consider three cases based on the relative lengths of A and B:\n\nLength of A equals length of B\nSince B+ \n′\n b \n′\n +C is a prefix of str \n2\n​\n =A+ \n′\n a \n′\n +B+ \n′\n b \n′\n +C, this implies 'b' = 'a', which is impossible.\n\nLength of A is greater than length of B\nSince both A and B+ \n′\n b \n′\n +C are prefixes of str \n2\n​\n , let A=B+ \n′\n b \n′\n +D.\nBecause A is also a suffix of A+ \n′\n a \n′\n +B, we obtain:\nB+ \n′\n b \n′\n +D=D+ \n′\n a \n′\n +B\nRemoving common parts leads to 'b' = 'a', which is impossible.\n\nLength of A is less than length of B\nSince A is a suffix of A+ \n′\n a \n′\n +B, let B=D+A.\nSince B+ \n′\n b \n′\n +C is a prefix of str \n2\n​\n , we obtain:\n(D+A)+ \n′\n b \n′\n =A+ \n′\n a \n′\n +D\nAgain, this leads to 'b' = 'a', which is impossible.\n\nIn all cases, we reach a contradiction. Therefore, modifying the rightmost non-fixed character will never violate any previously satisfied 'F' constraint, while also minimizing the lexicographical impact.",
        "cpp": "class Solution {\npublic:\n    string generateString(string str1, string str2) {\n        int n = str1.size(), m = str2.size();\n        string s(n + m - 1, 'a');\n        vector<int> fixed(n + m - 1, 0);\n        for (int i = 0; i < n; i++) {\n            if (str1[i] == 'T') {\n                for (int j = i; j < i + m; j++) {\n                    if (fixed[j] && s[j] != str2[j - i]) {\n                        return \"\";\n                    } else {\n                        s[j] = str2[j - i];\n                        fixed[j] = 1;\n                    }\n                }\n            }\n        }\n\n        for (int i = 0; i < n; i++) {\n            if (str1[i] == 'F') {\n                bool flag = false;\n                int idx = -1;\n                for (int j = i + m - 1; j >= i; j--) {\n                    if (str2[j - i] != s[j]) {\n                        flag = true;\n                    }\n                    if (idx == -1 && !fixed[j]) {\n                        idx = j;\n                    }\n                }\n                if (flag) {\n                    continue;\n                } else if (idx != -1) {\n                    s[idx] = 'b';\n                } else {\n                    return \"\";\n                }\n            }\n        }\n        return s;\n    }\n};",
        "java": "class Solution {\n\n    public String generateString(String str1, String str2) {\n        int n = str1.length();\n        int m = str2.length();\n        char[] s = new char[n + m - 1];\n        int[] fixed = new int[n + m - 1];\n\n        for (int i = 0; i < s.length; i++) {\n            s[i] = 'a';\n        }\n\n        for (int i = 0; i < n; i++) {\n            if (str1.charAt(i) == 'T') {\n                for (int j = i; j < i + m; j++) {\n                    if (fixed[j] == 1 && s[j] != str2.charAt(j - i)) {\n                        return \"\";\n                    } else {\n                        s[j] = str2.charAt(j - i);\n                        fixed[j] = 1;\n                    }\n                }\n            }\n        }\n\n        for (int i = 0; i < n; i++) {\n            if (str1.charAt(i) == 'F') {\n                boolean flag = false;\n                int idx = -1;\n                for (int j = i + m - 1; j >= i; j--) {\n                    if (str2.charAt(j - i) != s[j]) {\n                        flag = true;\n                    }\n                    if (idx == -1 && fixed[j] == 0) {\n                        idx = j;\n                    }\n                }\n                if (flag) {\n                    continue;\n                } else if (idx != -1) {\n                    s[idx] = 'b';\n                } else {\n                    return \"\";\n                }\n            }\n        }\n        return new String(s);\n    }\n}",
        "python": "class Solution:\n    def generateString(self, str1: str, str2: str) -> str:\n        n, m = len(str1), len(str2)\n        s = [\"a\"] * (n + m - 1)\n        fixed = [False] * (n + m - 1)\n\n        # process the case of 'T'\n        for i, ch in enumerate(str1):\n            if ch == \"T\":\n                for j, c in enumerate(str2, i):\n                    if fixed[j] and s[j] != c:\n                        return \"\"\n                    s[j], fixed[j] = c, True\n\n        # process the case of 'F'\n        for i, ch in enumerate(str1):\n            if ch == \"F\":\n                # check if there are already different characters\n                if any(str2[j - i] != s[j] for j in range(i, i + m)):\n                    continue\n\n                # find the first modifiable position\n                for j in range(i + m - 1, i - 1, -1):\n                    if not fixed[j]:\n                        s[j] = \"b\"\n                        break\n                else:\n                    return \"\"\n\n        return \"\".join(s)",
        "timeComplexity": "O(nm)",
        "spaceComplexity": "O(n+m)",
        "date": "Mar 31, 2026"
    },
    {
        "id": "2069",
        "title": "Walking Robot Simulation II",
        "difficulty": "Medium",
        "tags": "senior , Design , Simulation",
        "description": "A width x height grid is on an XY-plane with the bottom-left cell at (0, 0) and the top-right cell at (width - 1, height - 1). The grid is aligned with the four cardinal directions (\"North\", \"East\", \"South\", and \"West\"). A robot is initially at cell (0, 0) facing direction \"East\".\n\nThe robot can be instructed to move for a specific number of steps. For each step, it does the following.\n\nAttempts to move forward one cell in the direction it is facing.\nIf the cell the robot is moving to is out of bounds, the robot instead turns 90 degrees counterclockwise and retries the step.\nAfter the robot finishes moving the number of steps required, it stops and awaits the next instruction.\n\nImplement the Robot class:\n\nRobot(int width, int height) Initializes the width x height grid with the robot at (0, 0) facing \"East\".\nvoid step(int num) Instructs the robot to move forward num steps.\nint[] getPos() Returns the current cell the robot is at, as an array of length 2, [x, y].\nString getDir() Returns the current direction of the robot, \"North\", \"East\", \"South\", or \"West\".\n \n\nExample 1:\n\nexample-1\nInput\n[\"Robot\", \"step\", \"step\", \"getPos\", \"getDir\", \"step\", \"step\", \"step\", \"getPos\", \"getDir\"]\n[[6, 3], [2], [2], [], [], [2], [1], [4], [], []]\nOutput\n[null, null, null, [4, 0], \"East\", null, null, null, [1, 2], \"West\"]\n\nExplanation\nRobot robot = new Robot(6, 3); // Initialize the grid and the robot at (0, 0) facing East.\nrobot.step(2);  // It moves two steps East to (2, 0), and faces East.\nrobot.step(2);  // It moves two steps East to (4, 0), and faces East.\nrobot.getPos(); // return [4, 0]\nrobot.getDir(); // return \"East\"\nrobot.step(2);  // It moves one step East to (5, 0), and faces East.\n                // Moving the next step East would be out of bounds, so it turns and faces North.\n                // Then, it moves one step North to (5, 1), and faces North.\nrobot.step(1);  // It moves one step North to (5, 2), and faces North (not West).\nrobot.step(4);  // Moving the next step North would be out of bounds, so it turns and faces West.\n                // Then, it moves four steps West to (1, 2), and faces West.\nrobot.getPos(); // return [1, 2]\nrobot.getDir(); // return \"West\"\n\n \n\nConstraints:\n\n2 <= width, height <= 100\n1 <= num <= 105\nAt most 104 calls in total will be made to step, getPos, and getDir.",
        "approach": "Approach: Simulation\nIntuition\nFrom the problem description, we can observe that the robot always moves in a loop along the outermost layer of the grid.\n\nfig1\n\nTherefore, we can preprocess the robot’s movement cycle (positions and directions) and store it in an array. We use a pointer idx to represent the robot’s current position and direction.\n\nThe preprocessing can be divided into four steps. As shown in the figure above, grids of different colors represent the robot’s direction at those positions. Accordingly, we can use four loops to enumerate the positions for each direction and store them in the preprocessing arrays.\n\nFor the three interfaces required by the problem, we can implement them as follows:\n\nvoid move(int num): Increase idx by num. Since the robot’s path is cyclic, we take the result modulo the length of the cycle.\n\nint[] getPos(): Return the position from the preprocessed array based on idx.\n\nString getDir(): Return the direction from the preprocessed array based on idx.\n\nNote that when the robot returns to the origin, its direction becomes 'South', whereas its initial direction at the origin is 'East'. To handle this, we set the direction of the origin in the preprocessing array to 'South' and use a boolean variable to track whether the robot has moved:\n\nIf the robot has not moved, we always return 'East'.\n\nIf the robot has moved, we return the direction from the preprocessing array based on idx.",
        "cpp": "class Robot {\nprivate:\n    bool moved = false;\n    int idx = 0;\n    vector<pair<int, int>> pos;\n    vector<int> dir;\n    unordered_map<int, string> to_dir = {\n        {0, \"East\"}, {1, \"North\"}, {2, \"West\"}, {3, \"South\"}};\n\npublic:\n    Robot(int width, int height) {\n        for (int i = 0; i < width; ++i) {\n            pos.emplace_back(i, 0);\n            dir.emplace_back(0);\n        }\n        for (int i = 1; i < height; ++i) {\n            pos.emplace_back(width - 1, i);\n            dir.emplace_back(1);\n        }\n        for (int i = width - 2; i >= 0; --i) {\n            pos.emplace_back(i, height - 1);\n            dir.emplace_back(2);\n        }\n        for (int i = height - 2; i > 0; --i) {\n            pos.emplace_back(0, i);\n            dir.emplace_back(3);\n        }\n        dir[0] = 3;\n    }\n\n    void step(int num) {\n        moved = true;\n        idx = (idx + num) % pos.size();\n    }\n\n    vector<int> getPos() { return {pos[idx].first, pos[idx].second}; }\n\n    string getDir() {\n        if (!moved) {\n            return \"East\";\n        }\n        return to_dir[dir[idx]];\n    }\n};",
        "java": "class Robot {\n\n    private boolean moved = false;\n    private int idx = 0;\n    private List<int[]> pos = new ArrayList<>();\n    private List<Integer> dir = new ArrayList<>();\n    private Map<Integer, String> toDir = new HashMap<>();\n\n    public Robot(int width, int height) {\n        toDir.put(0, \"East\");\n        toDir.put(1, \"North\");\n        toDir.put(2, \"West\");\n        toDir.put(3, \"South\");\n\n        for (int i = 0; i < width; ++i) {\n            pos.add(new int[] { i, 0 });\n            dir.add(0);\n        }\n        for (int i = 1; i < height; ++i) {\n            pos.add(new int[] { width - 1, i });\n            dir.add(1);\n        }\n        for (int i = width - 2; i >= 0; --i) {\n            pos.add(new int[] { i, height - 1 });\n            dir.add(2);\n        }\n        for (int i = height - 2; i > 0; --i) {\n            pos.add(new int[] { 0, i });\n            dir.add(3);\n        }\n        dir.set(0, 3);\n    }\n\n    public void step(int num) {\n        moved = true;\n        idx = (idx + num) % pos.size();\n    }\n\n    public int[] getPos() {\n        return pos.get(idx);\n    }\n\n    public String getDir() {\n        if (!moved) {\n            return \"East\";\n        }\n        return toDir.get(dir.get(idx));\n    }\n}",
        "python": "class Robot:\n\n    TO_DIR = {\n        0: \"East\",\n        1: \"North\",\n        2: \"West\",\n        3: \"South\",\n    }\n\n    def __init__(self, width: int, height: int):\n        self.moved = False\n        self.idx = 0\n        self.pos = list()\n        self.dirs = list()\n\n        pos_, dirs_ = self.pos, self.dirs\n\n        for i in range(width):\n            pos_.append((i, 0))\n            dirs_.append(0)\n        for i in range(1, height):\n            pos_.append((width - 1, i))\n            dirs_.append(1)\n        for i in range(width - 2, -1, -1):\n            pos_.append((i, height - 1))\n            dirs_.append(2)\n        for i in range(height - 2, 0, -1):\n            pos_.append((0, i))\n            dirs_.append(3)\n\n        dirs_[0] = 3\n\n    def step(self, num: int) -> None:\n        self.moved = True\n        self.idx = (self.idx + num) % len(self.pos)\n\n    def getPos(self) -> List[int]:\n        return list(self.pos[self.idx])\n\n    def getDir(self) -> str:\n        if not self.moved:\n            return \"East\"\n        return Robot.TO_DIR[self.dirs[self.idx]]",
        "timeComplexity": "O(1)",
        "spaceComplexity": "O(width+height)",
        "date": "Apr 7, 2026"
    }
];
